import React from 'react'; //라이브러리 (where ? 모듈에서(cra할때))
import ReactDOM from 'react-dom/client';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
//root div에 그려줘 app이라는 것을

